pipeline {
    agent any

    stages {
        stage('Terraform Deploy') {
            steps {
                dir('terraform') {
                    sh '''
                    terraform init -reconfigure
                    terraform apply -auto-approve
                    '''
                }
            }
        }
    }
}