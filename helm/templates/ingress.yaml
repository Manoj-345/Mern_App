apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: quickchat-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: quickchat.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: quickchat-frontend
                port:
                  number: 80