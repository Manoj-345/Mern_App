{
  "title": "QuickChat Kubernetes Dashboard",
  "panels": [
    {
      "type": "graph",
      "title": "Pod CPU Usage",
      "targets": [
        {
          "expr": "sum(rate(container_cpu_usage_seconds_total[5m])) by (pod)"
        }
      ]
    },
    {
      "type": "graph",
      "title": "Pod Memory Usage",
      "targets": [
        {
          "expr": "sum(container_memory_usage_bytes) by (pod)"
        }
      ]
    }
  ],
  "schemaVersion": 36,
  "version": 1
}